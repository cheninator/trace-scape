{
  "name": "trace-scape",
  "version": "1.0.0",
  "description": "A trace viewer built with web technologies",
  "main": "main.js",
  "scripts": {
    "build": "gulp build",
    "bundle": "echo",
    "clean": "rimraf bin",
    "clean:modules": "rimraf node_modules",
    "desktop": "electron bin/electron",
    "start": "npm run clean && tsc && npm run build && concurrently --kill-others \"tsc -w\" \"npm run web\" ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "web": "lite-server"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cheninator/trace-scape"
  },
  "author": "Yonni Chen",
  "license": "Apache-2.0",
  "dependencies": {
    "chart.js":"^2.7.1",
    "concurrently": "3.5.0",
    "jquery": "^3.2.1",
    "pixi.js": "^4.6.0"
  },
  "devDependencies": {
    "@types/chartjs": "^0.0.31",
    "@types/jquery": "^3.2.16",
    "@types/pixi.js": "^4.5.7",
    "electron": "^1.7.8",
    "gulp": "^3.9.1",
    "gulp-flatten": "^0.3.1",
    "gulp-uglify": "^3.0.0",
    "lite-server": "^2.3.0",
    "rimraf": "^2.5.4",
    "tslint": "^5.8.0",
    "tslint-eslint-rules": "^4.1.1",
    "typescript": "^2.5.3"
  }
}
